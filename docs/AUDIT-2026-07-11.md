# Pre-launch audit — 2026-07-11 (static pass, battery-limited)

Scope: full test suites, lint/compile, security scan, repo hygiene. NOT covered
(needs the live pass tomorrow): actually launching lean/full builds, onboarding
flow, real-Pepper bridge, voice loop, full-build model download path.

## Verified green
- **Tests: 249/249 pass** — 165 python (brain+simulator, 21s), 84 frontend (vitest, 8s)
- All python compiles; zero TODO/FIXME left in source
- No hardcoded secrets/keys (env-based config throughout)
- Network posture correct for a local app: bridge + websocket bind 127.0.0.1
  only, CORS allowlist = localhost origins only
- Repo hygiene: gitignore covers build artifacts, no junk tracked, LICENSE (MIT) in
- README covers unsigned-build UX for macOS/Windows (the #1 support question)

## Fix before posting anywhere (blockers)
1. **Publish the releases.** README's download table links to an empty
   releases page — every visitor hits a dead end. This is THE launch blocker.
   Lean + full builds for the 3 OSes, versioned (v0.x), README table matches
   actual asset names.
2. **Live smoke pass on each shipped binary** (tomorrow, on AC): unzip →
   launch → onboarding → sim drives → API answers on :5001 → voice loop.
   249 unit tests ≠ the zip actually starting on a clean machine.

## Should fix (pre- or same-week as launch)
3. **Error-states polish** (already the known last UI item — carried from
   before, still open).
4. **29 broad `except Exception` handlers** in brain/ + simulator/ non-test
   code. Most are probably fine (hardware/LLM calls need catch-alls), but
   triage the user-facing ones: a swallowed exception during onboarding or
   provisioning = silent hang a new user can't diagnose. Rule: catch-all is OK
   only if it logs + surfaces state to the UI.
5. **`report.zip` in repo root** (untracked) — delete or move out; decide
   whether report/ (the Hausarbeit HTMLs) belongs in a public dev-tool repo.

## Nice-to-have (post-launch)
6. Doc sprawl: CONNECT_REAL_PEPPER / DEPLOYMENT / LLAMA_SETUP / VOICE_LOOP as
   root-level MDs — fold into docs/ with a README index once strangers arrive.
7. Vite dev server binds 0.0.0.0 (dev-only file, but tighten to localhost
   unless LAN-testing the UI from the phone is wanted).
8. ~~docs/ is in .gitignore~~ — NOT a bug: repo convention (CLAUDE.md) keeps
   local specs/plans unpublished on purpose. This audit was force-added as a
   deliberate exception. No action.
9. No CI — a GitHub Action running both suites on push would guard the
   first external PRs. Cheap credibility signal for the OSS application too.

## Launch-day order
releases published → smoke pass on each OS binary → error states → THEN reddit/forums.
