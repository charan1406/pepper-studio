# Core — required to run the simulator and serve the UI.
websockets>=12.0
# Robot connection over SSH (deploy + run bridge.py on a real Pepper from the UI).
paramiko

# Optional hardware proxies: use the laptop webcam/mic as Pepper's camera/mic.
# The simulator runs fine without these (camera shows a placeholder, mic is
# silent). Install with: pip install opencv-python numpy pyaudio
# opencv-python>=4.8.0
# numpy>=1.24.0
# pyaudio>=0.2.13

# Optional local speech-to-text (faster-whisper). Powers the voice loop and the
# in-app Voice panel (transcribes Pepper's mic so the AI brain can hear you).
# Without it, voice runs but has no input. Weights auto-download on first use
# (~460MB). Bundled into the "voice" packaged build only (./build.sh voice — see
# pepper-studio.spec); install it to run voice from source:
# faster-whisper>=1.2.0

# Optional natural voice (Piper neural TTS, run as a sidecar process). Without
# these, speech falls back to the browser's built-in TTS. Also needs the `aplay`
# binary (alsa-utils) and a voice .onnx model; point at it with SIM_PIPER_MODEL.
# piper-tts>=1.4.0
# flask>=3.0.0
